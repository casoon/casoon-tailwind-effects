{
  "name": "casoon-tailwind-effects",
  "private": true,
  "version": "0.7.1",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm@9.0.0",
  "license": "MIT",
  "scripts": {
    "test": "echo 'All packages use pure CSS - no tests needed'",
    "release:all": "echo 'Use pnpm publish -r for publishing all packages'",
    "dev": "cd test-project && npm run dev",
    "build:test": "cd test-project && npm run build"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.13"
  }
}
